const gridSize=21;export const randomGridPosition=()=>({x:Math.floor(21*Math.random())+1,y:Math.floor(21*Math.random())+1});export const outsideGrid=position=>position.x<1||position.x>21||position.y<1||position.y>21;